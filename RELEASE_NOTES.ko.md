## 이번 업데이트 (긴급 수정)

- **v2.35.2에서 6성 레이드를 한 번이라도 도전했던 세이브가 업데이트 후 초기화되던 결함을 고쳤습니다.** 지금 이 버전으로 업데이트하면 정상 세이브가 안전하게 불러와집니다.
- v2.35.2에서 초기화를 겪으셨다면: 앱을 종료하고 `~/Library/Application Support/PokeTokenBar/` 폴더에서 `companion-state.pre-reset-숫자.json` 파일을 찾아 `companion-state.json`으로 이름을 바꿔 덮어쓴 뒤 앱을 다시 여시면 됩니다(원본은 자동으로 백업되어 있습니다).
